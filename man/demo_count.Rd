% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo_count.R
\name{demo_count}
\alias{demo_count}
\title{Summarize demographic proportions for Lucid/Cint tracker data}
\usage{
demo_count(data, demo, date_break = NULL)
}
\arguments{
\item{data}{A list containing survey datasets. Expected to include at least:
\describe{
\item{\code{unweighted}}{A survey object or data frame compatible with
\code{srvyr} verbs.}
\item{\code{campaign}}{A survey object or data frame compatible with \code{srvyr}
verbs and containing \code{matched_control_xmedia}.}
}}

\item{demo}{A character string giving the name of the demographic variable
to group by.}

\item{date_break}{An optional character string giving the name of an
additional grouping variable, typically a date-derived period such as
\code{"quarter"}, \code{"month"}. Defaults to \code{NULL}.}
}
\value{
A named list with two elements:
\describe{
\item{\code{unweighted}}{A tibble of survey proportions and totals by the
requested grouping variables for \code{data$unweighted}.}
\item{\code{control_exposed}}{A tibble of survey proportions and totals by
\code{matched_control_xmedia} and the requested grouping variables for
\code{data$campaign}, excluding rows where \code{matched_control_xmedia} is
\code{"unclassified"}.}
}

Each returned tibble includes:
\describe{
\item{grouping columns}{The variables supplied in \code{demo} and optionally
\code{date_break}.}
\item{\code{proportion}}{Estimated survey proportion from
\code{srvyr::survey_mean()}.}
\item{\code{n}}{Estimated survey total from \code{srvyr::survey_total()}.}
}

Because grouping uses \code{dplyr::across(dplyr::all_of(...))}, \code{demo} and
\code{date_break} should be supplied as character strings naming columns present
in the input data.
}
\description{
Computes survey proportions and totals by demographic group, with optional
additional grouping by a date-based break variable. The function returns
results for both the unweighted survey data and the campaign-level
control/exposed data.
}
\examples{
\dontrun{
demo_count(data = survey_data, demo = "demo_gender")

demo_count(
  data = survey_data,
  demo = "demo_race_ethnicity",
  date_break = "quarter"
)
}

}
