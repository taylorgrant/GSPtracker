% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_cleaner.R
\name{brand_cleaner}
\alias{brand_cleaner}
\title{Clean and standardize brand-level survey outputs}
\usage{
brand_cleaner(tbl, brand, wide = FALSE)
}
\arguments{
\item{tbl}{A data frame returned from \code{\link[=brand_summary]{brand_summary()}}, containing at minimum
\code{svy_q}, \code{proportion}, \code{n}, \code{total}, \code{question}, and \code{var}.}

\item{brand}{A character string specifying the brand of interest. Input is
case-insensitive and supports common aliases (e.g., "bmw", "mercedes",
"mb").}

\item{wide}{Logical. If \code{TRUE}, returns a wide-format data frame with
brand-prefixed column names (e.g., \code{BMW_proportion}). If \code{FALSE} (default),
returns a tidy data frame with a \code{brand} column.}
}
\value{
A tibble containing cleaned brand-level results:
\itemize{
\item In tidy format (\code{wide = FALSE}): includes a \code{brand} column and
standard metric columns (\code{proportion}, \code{n}, \code{total}).
\item In wide format (\code{wide = TRUE}): metric columns are prefixed with the
brand name (e.g., \code{BMW_proportion}, \code{BMW_n}, \code{BMW_total}).
}
}
\description{
\code{brand_cleaner()} filters and reshapes the output of \code{\link[=brand_summary]{brand_summary()}}
for a specific brand, standardizing brand names, relabeling key responses,
and optionally returning a wide format for easier side-by-side comparison
across brands.
}
\details{
The function maps common user inputs (e.g., "bmw", "mercedes", "mb")
to canonical brand names, filters the relevant survey responses, and
prepares the data for downstream analysis or visualization.

The function:
\itemize{
\item Normalizes user-supplied brand names using a lookup table.
\item Filters to the selected brand and relevant derived categories
(e.g., "On its way up - Top 2 Box").
\item Replaces the brand label in \code{svy_q} with the corresponding question
text for readability.
\item Optionally reshapes output for easier comparison across brands.
}
}
\examples{
\dontrun{
# Tidy output
brand_cleaner(tbl, "bmw")

# Wide output for joining multiple brands
dplyr::left_join(
  brand_cleaner(tbl, "bmw", wide = TRUE),
  brand_cleaner(tbl, "audi", wide = TRUE),
  by = c("month", "matched_control_xmedia", "svy_q")
)
}

}
