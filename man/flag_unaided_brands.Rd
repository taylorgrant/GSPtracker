% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{flag_unaided_brands}
\alias{flag_unaided_brands}
\title{Flag unaided brand mentions from survey responses}
\usage{
flag_unaided_brands(data, cols = dplyr::starts_with("awr_u_"))
}
\arguments{
\item{data}{A data frame containing a unique respondent identifier and one
or more unaided awareness columns (e.g., \code{awr_u_1}, \code{awr_u_2}, etc.).}

\item{cols}{Tidy-select specification for the unaided awareness columns.
Defaults to \code{dplyr::starts_with("awr_u_")}.}
}
\value{
A tibble with one row per respondent and logical indicator columns for each
standardized brand (e.g., \code{awr_ua_BMW}, \code{awr_ua_Audi}). Each column is \code{TRUE}
if the respondent mentioned the brand in any unaided awareness field and
\code{FALSE} otherwise.
}
\description{
\code{flag_unaided_brands()} processes unaided awareness responses across multiple
columns (e.g., \code{awr_u_1} to \code{awr_u_n}), standardizes brand names, and returns
respondent-level indicator variables for whether each tracked brand was
mentioned.
}
\details{
The function reshapes the data to long format, applies
\code{standardize_brand()} to handle case differences and common misspellings,
and then summarizes results back to one row per respondent.

The function:
\itemize{
\item pivots unaided awareness columns to long format
\item standardizes raw responses using \code{standardize_brand()}
\item removes non-matching or invalid responses (e.g., \code{-99})
\item collapses back to respondent level with one column per brand
}

A unique respondent identifier (e.g., \code{response_id}) must be present in
the input data to ensure correct aggregation.
}
\examples{
\dontrun{
flags <- flag_unaided_brands(df)

df_out <- df |>
  dplyr::left_join(flags, by = "response_id")
}

}
\keyword{internal}
