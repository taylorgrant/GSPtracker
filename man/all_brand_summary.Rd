% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_brand_summary.R
\name{all_brand_summary}
\alias{all_brand_summary}
\title{Summarize and compare all tracked brands against BMW}
\usage{
all_brand_summary(
  data,
  groups = NULL,
  include_month = FALSE,
  include_quarter = FALSE
)
}
\arguments{
\item{data}{Dataset to be used. Likely the \code{unweighted} data.}

\item{groups}{Optional character vector of grouping variables to include in
the summaries before testing.}

\item{include_month}{Logical. If \code{TRUE}, includes monthly groupings in the
summaries and downstream comparisons.}

\item{include_quarter}{Logical. If \code{TRUE}, includes quarterly groupings in
the summaries and downstream comparisons.}
}
\value{
A tibble with one row per metric and grouping combination. Output includes:
\itemize{
\item grouping columns carried through from the summary step, such as
\code{question_group}, \code{svy_q}, and optionally \code{month}, \code{quarter}, or user-supplied
grouping variables
\item \code{bmw_est}: BMW estimate for the metric
\item one estimate column per comparison brand, such as
\code{Audi_other_est}, \code{Lexus_other_est}, etc.
\item one significance label column per comparison brand, such as
\code{Audi_sig_label}, \code{Lexus_sig_label}, etc.
}

Significance labels are coded as:
\itemize{
\item \code{"+"}: BMW is significantly higher than the comparison brand
\item \code{"-"}: BMW is significantly lower than the comparison brand
\item \code{""}: no statistically significant difference
}
}
\description{
\code{all_brand_summary()} runs \code{\link[=brand_summary]{brand_summary()}} across all tracked brands,
cleans the outputs with \code{\link[=brand_cleaner]{brand_cleaner()}}, performs pairwise proportion tests
within each metric, and returns a wide comparison table showing each brand's
estimate relative to BMW along with significance indicators.
}
\details{
The function compares Audi, BMW, Lexus, Mercedes Benz, and Tesla. Results are
tested within each grouping combination and reshaped so that non-BMW brands
appear in separate columns.

The function:
\itemize{
\item retrieves brand-specific tracker variables using \code{\link[=brand_choice]{brand_choice()}}
\item removes Unaided Awareness from \code{brand_vars}
\item summarizes each variable using \code{\link[=brand_summary]{brand_summary()}}
\item standardizes outputs using \code{\link[=brand_cleaner]{brand_cleaner()}}
\item performs pairwise \code{\link[stats:prop.test]{stats::prop.test()}} comparisons across brands
\item filters comparisons to those involving BMW
\item reshapes results into a wide comparison table
}

Pairwise tests are based on \code{n} and \code{total} returned by \code{\link[=brand_summary]{brand_summary()}}.
Because these are survey-based estimates, \code{prop.test()} should be interpreted
as an approximation rather than a fully design-based significance test.
}
\examples{
\dontrun{
# Overall BMW vs. other-brand comparisons
all_brand_summary()

# Quarterly comparisons
all_brand_summary(include_quarter = TRUE)

# Monthly comparisons by gender
all_brand_summary(
  groups = "demo_gender",
  include_month = TRUE
)
}

}
\seealso{
\code{\link[=brand_choice]{brand_choice()}}, \code{\link[=brand_summary]{brand_summary()}}, \code{\link[=brand_cleaner]{brand_cleaner()}}
}
